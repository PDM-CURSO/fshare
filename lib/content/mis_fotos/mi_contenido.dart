import 'package:flutter/material.dart';

class MiContenido extends StatelessWidget {
  const MiContenido({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("MI CONTENIDO"),
    );
  }
}
